# frontend/.env.local — leave empty for local dev (Vite proxies /api to port 3456).
# For AWS backend: copy Public IPv4 from Lightsail (four numbers, not "8988" alone).
# VITE_API_BASE_URL=http://52.14.89.88:8080
VITE_API_BASE_URL=
