{
  "name": "shop-style-crawler",
  "version": "1.0.0",
  "private": true,
  "description": "Shopify PDP style crawler — frontend + backend monorepo",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev:server": "npm run dev -w backend",
    "dev:client": "npm run dev -w frontend",
    "dev": "concurrently -n api,ui -c blue,magenta \"npm run dev:server\" \"npm run dev:client\"",
    "build": "npm run build -w frontend",
    "start:api": "npm run start:api -w backend",
    "docker:build": "docker build -t shop-crawler-api ./backend",
    "docker:run": "docker run --rm -p 8080:8080 -e NODE_ENV=production -e API_ONLY=true -e PORT=8080 shop-crawler-api",
    "crawl": "npm run crawl -w backend"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  },
  "license": "ISC"
}
